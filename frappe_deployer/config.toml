site_name = 'MySite'
mode = 'host'  # Can be 'host' or 'fm'

[[apps]]
repo = 'frappe/frappe'
ref = 'main'

[[apps]]
repo = 'frappe/erpnext'
ref = 'main'

[host]
bench_path = '/path/to/bench'

[fm]
# Add fm-specific configurations here
