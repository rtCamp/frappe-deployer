{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"Frappe Manager Deployer","text":"<p>fmd layers zero-downtime deployments on top of an existing Frappe Manager (FM) bench. Every deploy builds a new immutable, timestamped release; the live bench is a symlink repointed atomically, so switches are instant and rollback is a symlink away.</p> <ul> <li> <p> Get started</p> <p>Requirements, install, and a first deploy in minutes. One command from an FM bench to a live release.</p> </li> <li> <p> Deploy modes</p> <p>pull builds on the target server and switches there. ship builds locally or in CI, rsyncs the release, then configures and switches on the remote.</p> </li> <li> <p>:lucide-github:{ .lg .middle }   GitHub Actions</p> <p>Deploy on push with the <code>rtcamp/frappe-deployer</code> composite action. One input: <code>command: pull</code> or <code>command: ship</code>.</p> </li> <li> <p> Configuration</p> <p>TOML config for apps, build hooks, Frappe Cloud sync, restore-from-site, monorepo apps, remote workers, and maintenance-as-config.</p> </li> <li> <p> Concepts</p> <p>The mental model: releases, atomic symlink switch, shared persistent data, lifecycle, rollback, and config precedence.</p> </li> <li> <p> Maintenance mode</p> <p>Maintenance is config-only via <code>[switch]</code>: enable it and choose which phases (e.g. <code>migrate</code>) show a maintenance page.</p> </li> </ul>","path":["Frappe Manager Deployer"],"tags":[]},{"location":"#install","level":2,"title":"Install","text":"uv (recommended)pipxFrom source <pre><code>uv tool install frappe-deployer\n</code></pre> <pre><code>pipx install frappe-deployer\n</code></pre> <pre><code>git clone https://github.com/rtcamp/fmd.git\ncd fmd\npip install -e .\n</code></pre> <p>Requires Python &gt;=3.13,&lt;3.14, Docker + Docker Compose v2, and Frappe Manager with an existing bench for the site.</p>","path":["Frappe Manager Deployer"],"tags":[]},{"location":"#deploy-your-first-site","level":2,"title":"Deploy your first site","text":"<pre><code>fmd deploy pull site.localhost \\\n  --app frappe/frappe:version-15 \\\n  --app frappe/erpnext:version-15\n</code></pre> <p><code>deploy pull</code> configures the bench (if needed), builds a release, and switches to it atomically — rollback instantly if anything fails.</p> <p>Use a config file</p> <pre><code># site.toml holds your app list and settings\nfmd deploy pull --config site.toml\n</code></pre>","path":["Frappe Manager Deployer"],"tags":[]},{"location":"#where-to-go-next","level":2,"title":"Where to go next","text":"<p>New to fmd?</p> <p>Start with Getting Started, then read Concepts to understand releases and the symlink switch.</p> <p>Ready to deploy?</p> <p>See Deploy modes, Configuration, and GitHub Actions.</p>","path":["Frappe Manager Deployer"],"tags":[]},{"location":"BUILDING/","level":1,"title":"Building the Documentation","text":"<p>This guide explains how to build and preview the fmd documentation site locally.</p>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#prerequisites","level":2,"title":"Prerequisites","text":"<p>Install development dependencies:</p> <pre><code>cd /path/to/fmd\nuv sync --group dev\n</code></pre> <p>This installs: - zensical - MkDocs Material wrapper - mike - Documentation versioning - pytest and testing tools</p> <p>Alternatively, install globally:</p> <pre><code>uv tool install zensical\nuv tool install mike\n</code></pre> <p>Or use just commands (recommended):</p> <pre><code># Install just\nbrew install just  # macOS\ncargo install just  # Cross-platform via Rust\n\n# View available commands\njust\n</code></pre>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#build-the-docs","level":2,"title":"Build the Docs","text":"","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#development-server-live-reload","level":3,"title":"Development Server (Live Reload)","text":"<p>Start a live-reloading development server for local editing:</p> <pre><code>just docs\n# or\nuv run zensical serve -a 127.0.0.1:8000\n</code></pre> <p>Then open http://localhost:8000 in your browser.</p> <p>Changes to markdown files auto-reload. Note: This mode doesn't show the version selector.</p>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#preview-versioned-docs-locally","level":3,"title":"Preview Versioned Docs Locally","text":"<p>To preview with the version selector (requires deploying first):</p> <pre><code># First deploy a version\njust docs-deploy dev\n\n# Then serve versioned docs\njust docs-versioned\n# or\nuv run mike serve -F zensical.toml -a 127.0.0.1:8000\n</code></pre>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#build-static-site","level":3,"title":"Build Static Site","text":"<p>Build the static HTML site without deploying:</p> <pre><code>just docs-build\n# or\nuv run zensical build\n</code></pre> <p>Output goes to <code>site/</code> directory.</p>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#versioned-documentation-mike","level":2,"title":"Versioned Documentation (mike)","text":"<p>fmd uses mike to maintain multiple documentation versions.</p>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#deploy-development-version","level":3,"title":"Deploy Development Version","text":"<p>Deploy current code as <code>dev</code> version:</p> <pre><code>just docs-deploy dev\n# or\nversion=$(uv run python -c \"from fmd.__about__ import __version__; print(__version__)\")\nuv run mike deploy dev --title \"$version\" -F zensical.toml\n</code></pre>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#deploy-release-version","level":3,"title":"Deploy Release Version","text":"<p>Deploy and mark as latest (for releases):</p> <pre><code>just docs-release\n# or manually:\nuv run mike deploy --push --update-aliases v1.0 latest --title \"1.0.0\" -F zensical.toml\n</code></pre> <p>This creates: - Version alias: <code>v1.0</code> (major.minor) - Marks it as <code>latest</code> - Pushes to <code>gh-pages</code> branch</p>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#set-default-version","level":3,"title":"Set Default Version","text":"<p>Set which version users see by default:</p> <pre><code>just docs-default latest\n# or\nuv run mike set-default --push latest -F zensical.toml\n</code></pre>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#list-versions","level":3,"title":"List Versions","text":"<pre><code>just docs-list\n# or\nuv run mike list -F zensical.toml\n</code></pre>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#delete-a-version","level":3,"title":"Delete a Version","text":"<pre><code>just docs-delete v0.9\n# or\nuv run mike delete v0.9 -F zensical.toml\n</code></pre>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#documentation-structure","level":2,"title":"Documentation Structure","text":"<pre><code>docs/\n├── index.md              # Home page\n├── getting-started.md    # Requirements, install, quick start\n├── concepts.md           # Releases, symlink switch, layout, rollback\n├── configuration.md      # Config reference, hooks, restore, FC sync\n├── deploy-modes.md       # pull vs ship\n├── github-actions.md     # CI/CD\n├── commands/             # Command reference (index.md + auto-generated pages)\n│   ├── index.md\n│   ├── deploy.md         # generated\n│   ├── release.md        # generated\n│   └── ...               # generated\n├── troubleshooting.md\n├── faq.md\n├── changelog.md\n├── design/               # Design notes (not in nav)\n├── BUILDING.md           # This file\n└── assets/\n    └── stylesheets/extra.css\n</code></pre> <p>Command pages are generated</p> <p><code>commands/deploy.md</code>, <code>commands/release.md</code>, etc. are generated from the live CLI by <code>scripts/update_cli_docs.py</code> (run automatically in the docs CI workflow). Only <code>commands/index.md</code> is hand-written. Regenerate locally with:</p> <pre><code>uv run python scripts/update_cli_docs.py\n</code></pre>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#writing-documentation","level":2,"title":"Writing Documentation","text":"","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#style-guide","level":3,"title":"Style Guide","text":"<ul> <li>Use bold for UI elements, commands, and emphasis</li> <li>Use <code>code</code> for: commands, filenames, config keys, code snippets</li> <li>Use admonitions for important notes:</li> <li><code>!!! tip</code> for helpful hints</li> <li><code>!!! warning</code> for cautions</li> <li><code>!!! info</code> for general information</li> <li><code>!!! example</code> for examples</li> </ul>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#code-blocks","level":3,"title":"Code Blocks","text":"<p>Use fenced code blocks with language:</p> <pre><code>```bash\nfmd deploy pull --config site.toml\n```\n\n```toml\nsite_name = \"mysite.localhost\"\n```\n</code></pre>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#admonitions","level":3,"title":"Admonitions","text":"<pre><code>!!! tip \"Optional Title\"\n    This is a helpful tip.\n\n!!! warning\n    This is a warning.\n</code></pre>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#tabbed-content","level":3,"title":"Tabbed Content","text":"<pre><code>=== \"Tab 1\"\n    Content for tab 1\n\n=== \"Tab 2\"\n    Content for tab 2\n</code></pre>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#grid-cards","level":3,"title":"Grid Cards","text":"<pre><code>&lt;div class=\"grid cards\" markdown&gt;\n\n-   :material-icon:{ .lg .middle } &amp;nbsp; **[Title](link.md)**\n\n    ---\n\n    Description text here.\n\n&lt;/div&gt;\n</code></pre>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#configuration","level":2,"title":"Configuration","text":"<p>Documentation is configured in <code>zensical.toml</code>:</p> <ul> <li>Site metadata: name, description, URL</li> <li>Navigation: menu structure</li> <li>Theme: colors, features, icons</li> <li>Social links: GitHub, PyPI</li> </ul> <p>See the Zensical documentation for advanced configuration.</p>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#deployment-workflow","level":2,"title":"Deployment Workflow","text":"","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#automatic-via-github-actions","level":3,"title":"Automatic (via GitHub Actions)","text":"<p>Documentation is automatically deployed when:</p> <ol> <li>Push to <code>main</code> branch</li> <li>GitHub Actions builds docs with mike</li> <li>Deploys to <code>gh-pages</code> branch</li> </ol> <p>See <code>.github/workflows/docs.yml</code> for the workflow.</p>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#manual-deployment","level":3,"title":"Manual Deployment","text":"<p>For immediate deployment:</p> <pre><code>just docs-release\n</code></pre> <p>This: 1. Extracts version from <code>fmd/__about__.py</code> 2. Creates version slug (e.g., <code>v1.0</code> from <code>1.0.0</code>) 3. Deploys with aliases: <code>v1.0</code> and <code>latest</code> 4. Pushes to <code>gh-pages</code> branch</p>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#version-strategy","level":3,"title":"Version Strategy","text":"<ul> <li>Development: Deploy as <code>dev</code> (doesn't affect version history)</li> <li>Releases: Deploy as <code>vX.Y</code> + <code>latest</code> (e.g., <code>v1.0</code>, <code>v1.1</code>)</li> <li>Default: Set <code>latest</code> as default redirect</li> </ul> <p>Example workflow:</p> <pre><code># During development\njust docs-deploy dev\n\n# On release v1.0.0\njust docs-release  # Creates v1.0 + latest\n\n# On release v1.1.0\njust docs-release  # Creates v1.1 + latest (v1.0 still accessible)\n</code></pre>","path":["Building the Documentation"],"tags":[]},{"location":"BUILDING/#local-preview","level":2,"title":"Local Preview","text":"<p>To preview the exact site that will be deployed:</p> <pre><code>zensical build\ncd site\npython -m http.server 8000\n</code></pre> <p>Open http://localhost:8000.</p>","path":["Building the Documentation"],"tags":[]},{"location":"changelog/","level":1,"title":"Changelog","text":"<p>All notable changes to fmd will be documented in this file.</p> <p>The format is based on Keep a Changelog, and this project adheres to Semantic Versioning.</p>","path":["Changelog"],"tags":[]},{"location":"changelog/#unreleased","level":2,"title":"Unreleased","text":"","path":["Changelog"],"tags":[]},{"location":"changelog/#added","level":3,"title":"Added","text":"<ul> <li><code>fmd release shell</code> — interactive shell inside a release's build container.</li> <li><code>fmd release info</code> — inspect each app's git repo, branch, commit, and tag.</li> <li><code>[switch] restore_db_from_site</code> — restore the database from another local FM   site/bench at switch time, copying the source site's <code>encryption_key</code> into the target   so Fernet-encrypted secrets stay decryptable.</li> </ul>","path":["Changelog"],"tags":[]},{"location":"changelog/#changed","level":3,"title":"Changed","text":"<ul> <li><code>fmd info</code> is now <code>fmd release info</code>.</li> <li>The <code>[fm]</code> config section is merged into <code>[switch]</code> as <code>restore_db_from_site</code>.</li> </ul>","path":["Changelog"],"tags":[]},{"location":"changelog/#removed","level":3,"title":"Removed","text":"<ul> <li><code>fmd maintenance</code> command — maintenance mode is now config-only via <code>[switch]</code>   (<code>maintenance_mode</code> + <code>maintenance_mode_phases</code>); bypass tokens are gone.</li> </ul>","path":["Changelog"],"tags":[]},{"location":"changelog/#010-2026-04-xx","level":2,"title":"0.1.0 - 2026-04-XX","text":"","path":["Changelog"],"tags":[]},{"location":"changelog/#added_1","level":3,"title":"Added","text":"<ul> <li>Initial release of fmd (Frappe Manager Deployer)</li> <li>Atomic release management with timestamped releases</li> <li>Zero-downtime deployment via symlink switching</li> <li>Pull mode: build on production server</li> <li>Ship mode: build locally, deploy to remote</li> <li>Frappe Cloud integration (apps, deps, DB sync)</li> <li>Worker draining before release switch</li> <li>Config-driven maintenance mode</li> <li>Remote worker support</li> <li>Monorepo app support with symlinks</li> <li>Automatic rollback on failure</li> <li>Release cleanup and retention management</li> <li>Database search-replace utility</li> <li>Comprehensive hook system (8 build hooks, 4 switch hooks)</li> <li>GitHub Actions integration (pull and ship strategies)</li> <li>TOML configuration with all options documented</li> </ul>","path":["Changelog"],"tags":[]},{"location":"changelog/#dependencies","level":3,"title":"Dependencies","text":"<ul> <li>Python &gt;=3.13,&lt;3.14</li> <li>Docker + Docker Compose v2</li> <li>Frappe Manager</li> </ul>","path":["Changelog"],"tags":[]},{"location":"concepts/","level":1,"title":"Concepts","text":"<p>fmd (Frappe Manager Deployer) adds zero-downtime deployments to an existing Frappe Manager (FM) bench. Every deploy builds a new immutable, timestamped release and repoints a symlink atomically. Old releases stay on disk for instant rollback, and persistent state (DB, site files, configs, logs) lives once in <code>deployment-data/</code> and is symlinked into every release — so releases share data without duplicating it.</p>","path":["Concepts"],"tags":[]},{"location":"concepts/#atomic-releases-symlink-switching","level":2,"title":"Atomic releases &amp; symlink switching","text":"<p>A release is a self-contained bench directory named <code>release_YYYYMMDD_HHMMSS</code>. It holds its own app source, Python venv, and Node runtime, so releases never share build artifacts and cannot interfere with each other.</p> <p>The live bench is the symlink <code>workspace/frappe-bench</code>. Activating a release is a single atomic relink of that symlink to the target release directory. Because FM always looks at <code>frappe-bench</code>, switching is instantaneous and reversible:</p> <ul> <li>Switch forward = point <code>frappe-bench</code> at a new release.</li> <li>Roll back = point <code>frappe-bench</code> at an older release.</li> </ul> <p>You never edit these symlinks by hand; fmd manages them during <code>configure</code> and <code>switch</code>.</p>","path":["Concepts"],"tags":[]},{"location":"concepts/#release-lifecycle","level":2,"title":"Release lifecycle","text":"<p>A full deploy is three phases. <code>fmd deploy pull</code> runs all three in sequence; you can also run each <code>release</code> subcommand on its own.</p> Phase Command What it does configure <code>fmd release configure</code> One-time. Converts a plain FM bench into the fmd release layout: creates <code>deployment-data/</code> (persistent state), sets up the <code>frappe-bench</code> symlink, and prepares <code>deployment-backup/</code>. Idempotent — skipped if already configured. create <code>fmd release create</code> Builds a new <code>release_YYYYMMDD_HHMMSS/</code>: clones apps at their pinned refs, creates the release-scoped venv, installs Node, runs <code>bench build</code>, and runs build hooks. Does not touch the live symlink — safe to run while the site serves traffic. switch <code>fmd release switch &lt;release&gt;</code> Atomically activates a release: optionally drains workers and backs up the DB, enables maintenance mode for the configured phases, relinks <code>frappe-bench</code>, runs <code>bench migrate</code>, restarts services, then disables maintenance. <p>Behavior in each phase is driven by config: <code>[release]</code> governs <code>create</code>, <code>[switch]</code> governs <code>switch</code>. See Configuration.</p>","path":["Concepts"],"tags":[]},{"location":"concepts/#workspace-layout","level":2,"title":"Workspace layout","text":"<p>Verified on-disk layout for a configured site:</p> <pre><code>~/frappe/sites/&lt;site&gt;/workspace/\n  frappe-bench -&gt; release_YYYYMMDD_HHMMSS      # symlink to the ACTIVE release\n  deployment-data/                             # persistent across releases\n    sites/   config/   logs/\n  release_YYYYMMDD_HHMMSS/                      # one immutable release\n    apps/  env/ (venv)  .uv/  .fnm/  sites -&gt; ../deployment-data/sites   &lt;site&gt;.toml (snapshot)\n  .cache/\n~/frappe/sites/&lt;site&gt;/deployment-backup/\n  release_YYYYMMDD_HHMMSS/                      # pre-switch DB + config backups\n</code></pre> <ul> <li><code>frappe-bench</code> — symlink to the active release.</li> <li><code>release_YYYYMMDD_HHMMSS/</code> — one immutable release. <code>apps/</code> holds app source,   <code>env/</code> is the release-scoped venv, <code>.uv/</code> and <code>.fnm/</code> are per-release Python/Node   caches, <code>sites</code> symlinks back to shared data, and <code>&lt;site&gt;.toml</code> is a snapshot of the   config used to build the release.</li> <li><code>deployment-data/</code> — persistent state shared by every release (see below).</li> <li><code>.cache/</code> — workspace-level caches reused across builds.</li> <li><code>deployment-backup/</code> — pre-switch DB and config backups, one directory per release.</li> </ul>","path":["Concepts"],"tags":[]},{"location":"concepts/#persistent-data-vs-releases","level":2,"title":"Persistent data vs releases","text":"<p>Releases are disposable; data is not. Everything durable lives once under <code>deployment-data/</code>:</p> <ul> <li><code>sites/</code> — site DB config, uploaded files, and per-site data.</li> <li><code>config/</code> — service/supervisor configuration.</li> <li><code>logs/</code> — application logs.</li> </ul> <p>Each release's <code>sites</code> entry is a symlink to <code>deployment-data/sites</code>, so all releases read and write the same site data. Building or deleting a release never changes the DB or uploaded files — only the code and runtimes change between releases.</p>","path":["Concepts"],"tags":[]},{"location":"concepts/#rollback-retention","level":2,"title":"Rollback &amp; retention","text":"<p>Rollback is just a switch to an older release. Because the previous release is still on disk, reverting is as fast as any switch:</p> <pre><code>fmd release list &lt;site&gt;                 # see available releases\nfmd release switch &lt;site&gt; &lt;older_release&gt;\n</code></pre> <p>Automatic rollback — with <code>[switch] rollback = true</code>, if the migration step fails during a switch, fmd reverts <code>frappe-bench</code> to the previous working release. With <code>rollback = false</code> (default) a failed switch leaves the site on the previous release while preserving the new release for debugging.</p> <p>Pre-switch backups — with <code>[switch] backups = true</code>, fmd backs up the DB and site configs into <code>deployment-backup/release_YYYYMMDD_HHMMSS/</code> before migrating, giving a restore point independent of the release directories.</p> <p>Retention — <code>[release] releases_retain_limit</code> (default <code>7</code>) caps how many releases are kept. <code>fmd cleanup</code> prunes old releases and their backups:</p> <pre><code>fmd cleanup &lt;site&gt; -r 3 -b 5 -y         # keep 3 releases, 5 backups\n</code></pre>","path":["Concepts"],"tags":[]},{"location":"concepts/#configuration-precedence","level":2,"title":"Configuration precedence","text":"<p>Settings resolve highest-to-lowest:</p> <ol> <li>CLI flags — e.g. <code>--app</code>, <code>--mode</code>.</li> <li>Config file — values in <code>site.toml</code> (via <code>--config</code> or an auto-discovered file).</li> <li>Built-in defaults — from fmd's config models.</li> </ol> <p><code>bench_name</code> defaults to <code>site_name</code> when unset. For the full set of sections, keys, and defaults, see Configuration; for how the build/switch runs on-server vs. locally, see Deploy modes.</p>","path":["Concepts"],"tags":[]},{"location":"configuration/","level":1,"title":"Configuration","text":"<p>fmd is driven by a TOML config file (conventionally <code>site.toml</code>), passed with <code>-c/--config</code>. It replaces long command lines and makes deployments reusable and version-controllable.</p> <p>This page covers the most-used keys per section. The exhaustive, annotated schema lives in <code>example-config.toml</code> at the repo root — consult it for every field and default.</p>","path":["Configuration"],"tags":[]},{"location":"configuration/#config-file-basics","level":2,"title":"Config file basics","text":"<pre><code>site_name = \"mysite.localhost\"   # required\nbench_name = \"\"                   # FM bench/container name; defaults to site_name\ngithub_token = \"${GITHUB_TOKEN}\"  # PAT for private repos; empty = public only\nverbose = false                   # verbose output for all commands\n</code></pre> <ul> <li><code>site_name</code> is the only required key and must match a site in Frappe Manager.</li> <li><code>bench_name</code> defaults to <code>site_name</code>; set it only when the bench directory differs.</li> <li>String values support <code>${VAR}</code> / <code>$VAR</code> environment substitution; undefined   variables are left untouched. Non-string values are not substituted.</li> </ul> <p>Reference the file from any command:</p> <pre><code>fmd deploy pull --config site.toml\n</code></pre> <p>Precedence: CLI flags &gt; config file values &gt; built-in defaults.</p>","path":["Configuration"],"tags":[]},{"location":"configuration/#apps","level":2,"title":"Apps","text":"<p>Each app is a repeated <code>[[apps]]</code> block, installed in order:</p> <pre><code>[[apps]]\nrepo = \"frappe/frappe\"   # \"org/repo\" or full GitHub URL\nref = \"version-15\"        # branch, tag, or commit\n\n[[apps]]\nrepo = \"frappe/erpnext\"\nref = \"version-15\"\n</code></pre> <p>Key per-app fields:</p> Field Purpose <code>repo</code> <code>org/repo</code> or full URL <code>ref</code> branch, tag, or commit <code>subdir_path</code> path to the app inside a monorepo (e.g. <code>apps/my-app</code>) <code>symlink</code> symlink the app instead of copying (dev, or monorepo subdir apps) <p>Shorthand on the CLI via <code>--app</code>:</p> <pre><code>org/repo:ref            org/repo:ref:subdir/path\n</code></pre> <pre><code>fmd deploy pull mysite.localhost --app my-org/monorepo:main:apps/my-app\n</code></pre>","path":["Configuration"],"tags":[]},{"location":"configuration/#monorepo-apps","level":3,"title":"Monorepo apps","text":"<p>Point <code>subdir_path</code> at the app directory within a larger repo. fmd clones the full repo and exposes the subdirectory as a normal Frappe app; <code>symlink = true</code> is recommended for subdir apps. Set <code>[release] symlink_subdir_apps = true</code> to symlink all subdir apps at once.</p> <pre><code>[[apps]]\nrepo = \"my-org/monorepo\"\nref = \"main\"\nsubdir_path = \"apps/my-app\"\nsymlink = true\n</code></pre>","path":["Configuration"],"tags":[]},{"location":"configuration/#release","level":2,"title":"[release]","text":"<p>Controls how a release is built.</p> <pre><code>[release]\nreleases_retain_limit = 7   # older releases pruned automatically\npython_version = \"\"          # e.g. \"3.12\"; empty = system default\nnode_version = \"\"            # e.g. \"20\"; empty = fnm default\nplatform = \"\"                # e.g. \"linux/arm64\" for cross-arch builds\nmode = \"\"                    # \"exec\" (default) or \"image\"\n</code></pre> <ul> <li><code>mode = \"exec\"</code> builds in the running docker-compose containers; <code>image</code> builds in   a temporary container (works without running services, useful in CI). <code>--build-dir</code>   implies image mode.</li> <li><code>use_fc_apps</code> / <code>use_fc_deps</code> pull the app list and Python version from Frappe   Cloud (see Frappe Cloud sync).</li> <li>Global build hooks may be set here as fallbacks for apps without their own.</li> </ul>","path":["Configuration"],"tags":[]},{"location":"configuration/#switch","level":2,"title":"[switch]","text":"<p>Controls runtime behavior when a release is activated (<code>release switch</code>, and the switch step of <code>deploy pull</code>/<code>ship</code>).</p> <pre><code>[switch]\nmigrate = true                       # run bench migrate\nmigrate_timeout = 300\nmigrate_command = \"\"                 # override the migrate command\nmaintenance_mode = true              # see Maintenance mode below\nmaintenance_mode_phases = [\"migrate\"]\nbackups = true                       # DB backup before switch\nrollback = false                     # auto-revert to previous release on failure\nsearch_replace = true                # rewrite site name in DB after restore\ninstall_apps = true                  # install apps into the site during switch\n</code></pre> <ul> <li><code>migrate</code> runs <code>bench migrate</code> during the switch; <code>migrate_command</code> overrides it.</li> <li><code>backups</code> snapshots the DB into <code>deployment-backup/</code> before switching.</li> <li><code>rollback = true</code> silently reverts on failure — enable only in unattended   environments; otherwise fix forward or <code>fmd release switch</code> manually.</li> <li><code>install_apps = false</code> clones apps into the release but skips installing them into   the site (CLI: <code>--no-install-apps</code>).</li> <li>Worker draining: <code>drain_workers</code>, <code>drain_workers_timeout</code>, <code>skip_stale_workers</code>,   <code>worker_kill_timeout</code>, and related poll/timeout keys govern how background workers   are drained/killed before restart.</li> <li><code>[switch.common_site_config]</code> and <code>[switch.site_config]</code> merge key/value pairs into   the respective JSON files during switch.</li> <li>Restore keys <code>use_fc_db</code> and <code>restore_db_from_site</code> are covered in   Restoring a database.</li> </ul>","path":["Configuration"],"tags":[]},{"location":"configuration/#restoring-a-database","level":2,"title":"Restoring a database","text":"<p>Two ways to seed this site's DB at switch time:</p> <pre><code>[switch]\nuse_fc_db = true                          # restore latest Frappe Cloud backup\nrestore_db_from_site = \"other.localhost\"  # restore from another local FM bench/site\n</code></pre> <ul> <li><code>use_fc_db</code> downloads and restores the latest Frappe Cloud backup (requires   <code>[fc]</code> credentials).</li> <li><code>restore_db_from_site</code> exports the DB from another local FM bench/site and restores   it into this one.</li> </ul> <p>Encryption key handling</p> <p>For both flows, fmd copies the source site's <code>encryption_key</code> into the target <code>site_config.json</code> before <code>bench migrate</code>, so Fernet-encrypted secrets (API keys, SMTP/OAuth passwords in <code>__Auth</code>) stay decryptable. Frappe has no key-rotation command — copying the key is the documented method. When <code>search_replace</code> is on, the old site name is rewritten to the new one.</p> <p>This is distinct from <code>backup_encryption_key</code>, which only GPG-encrypts backup files when the <code>encrypt_backup</code> system setting is on — unrelated to this flow.</p>","path":["Configuration"],"tags":[]},{"location":"configuration/#frappe-cloud-sync","level":2,"title":"Frappe Cloud sync","text":"<pre><code>[fc]\napi_key = \"\"\napi_secret = \"\"\nsite_name = \"\"    # your FC site\nteam_name = \"\"\n\n[release]\nuse_fc_apps = true   # import app list + commit hashes from FC (merged with local)\nuse_fc_deps = true   # import python_version from FC (only if not set locally)\n\n[switch]\nuse_fc_db = true     # restore latest FC DB backup at switch\n</code></pre> <ul> <li><code>use_fc_apps</code> overrides local <code>[[apps]]</code> refs with FC commit hashes while   preserving local app config (hooks, <code>symlink</code>, <code>subdir_path</code>).</li> <li><code>use_fc_deps</code> sets <code>python_version</code> from FC only when not explicitly configured   locally.</li> </ul>","path":["Configuration"],"tags":[]},{"location":"configuration/#hooks","level":2,"title":"Hooks","text":"<p>Hooks are inline shell scripts run at specific lifecycle points. Values starting with <code>host_</code> run on the host; the rest run inside the build/switch container.</p> <p>Build hooks — 8 per app (also settable globally under <code>[release]</code> as fallbacks):</p> <pre><code>[[apps]]\nrepo = \"my-org/my-app\"\nref = \"main\"\nbefore_bench_build = \"\"\"\nnpm ci\nnpm run build:prod\n\"\"\"\nhost_after_bench_build = 'curl -X POST \"$WEBHOOK_URL\" -d done'\n</code></pre> Container Host <code>before_bench_build</code> <code>host_before_bench_build</code> <code>after_bench_build</code> <code>host_after_bench_build</code> <code>before_python_install</code> <code>host_before_python_install</code> <code>after_python_install</code> <code>host_after_python_install</code> <p>Restart hooks — 4 under <code>[switch]</code>:</p> <pre><code>[switch]\nbefore_restart = \"bench --site all clear-cache\"\nhost_after_restart = \"curl https://healthcheck.io/ping/xyz\"\n</code></pre> <p><code>before_restart</code>, <code>after_restart</code> (container) and <code>host_before_restart</code>, <code>host_after_restart</code> (host).</p>","path":["Configuration"],"tags":[]},{"location":"configuration/#maintenance-mode","level":2,"title":"Maintenance mode","text":"<p>Maintenance mode is configuration only — there is no <code>fmd maintenance</code> command and no bypass tokens. It is controlled entirely by <code>[switch]</code>:</p> <pre><code>[switch]\nmaintenance_mode = true\nmaintenance_mode_phases = [\"migrate\"]   # valid: \"drain\", \"migrate\"\n</code></pre> <ul> <li><code>maintenance_mode</code> enables maintenance during switch operations.</li> <li><code>maintenance_mode_phases</code> restricts when it is active. <code>[\"migrate\"]</code> (default)   enables it only during migration; <code>[\"drain\", \"migrate\"]</code> covers both phases; an   empty list applies it to all phases.</li> </ul> <p>For zero-downtime code-only deploys, set <code>maintenance_mode = false</code> and <code>migrate = false</code>. For schema changes, keep both on.</p>","path":["Configuration"],"tags":[]},{"location":"configuration/#remote-workers","level":2,"title":"Remote workers","text":"<p>Run background workers on a separate server, synced from the primary.</p> <pre><code>[remote_worker]\nserver_ip = \"\"        # IP/domain of the worker server\nssh_user = \"frappe\"\nssh_port = 22\ninclude_dirs = []      # extra directories to sync\ninclude_files = []     # extra files to sync (e.g. \".env\")\n</code></pre> <p>Enable ports and sync releases:</p> <pre><code>fmd remote-worker enable mysite.localhost --rw-server 192.168.1.100\nfmd remote-worker sync   mysite.localhost --rw-server 192.168.1.100\n</code></pre> <p><code>enable</code> opens the Redis/MariaDB ports the worker needs; <code>sync</code> copies the active release to it. Set <code>[switch] sync_workers = true</code> to sync automatically after each successful switch.</p>","path":["Configuration"],"tags":[]},{"location":"configuration/#ship-remote","level":2,"title":"Ship &amp; remote","text":"<p>Ship mode builds the release locally or in CI and rsyncs it to a remote host, which only needs SSH + rsync + uv.</p> <pre><code>[ship]\nhost = \"\"              # remote hostname/IP (required for ship)\nssh_user = \"frappe\"\nssh_port = 22\nremote_path = \"\"       # empty = auto-detect $HOME/frappe/sites/&lt;site&gt;\nfmd_source = \"\"        # git URL / local path to run fmd remotely via uvx; empty = auto\nrsync_options = []     # extra rsync flags, e.g. [\"--bwlimit=10000\"]\n</code></pre> <p>See Deploy modes for pull vs ship, and GitHub Actions for CI/CD.</p>","path":["Configuration"],"tags":[]},{"location":"deploy-modes/","level":1,"title":"Deploy Modes","text":"<p>fmd deploys in two ways: pull and ship. Both produce the same immutable, timestamped release and atomic symlink switch (see Concepts); they differ only in where the build runs and what the target server must provide.</p> Mode Build location Deploy target When to use pull The target server Same server Target has build capacity and git/registry access; simplest setup ship Local machine or CI (Docker runner image) Remote via SSH + rsync Offload the build; target should stay lean (SSH + rsync + uv only)","path":["Deploy Modes"],"tags":[]},{"location":"deploy-modes/#pull","level":2,"title":"Pull","text":"<p>Build on the target server and switch there in one step.</p> <pre><code>fmd deploy pull mysite.localhost \\\n  --app frappe/frappe:version-15 \\\n  --app frappe/erpnext:version-15\n</code></pre> <p>Or drive everything from config:</p> <pre><code>fmd deploy pull --config site.toml\n</code></pre> <p><code>deploy pull</code> runs <code>release configure</code> (if the bench is not yet in fmd layout), then <code>release create</code> and <code>release switch</code> on the target. Because the build happens locally on that server, it needs enough CPU/RAM to build inside Docker plus network access to git and any container registries it pulls from.</p> <p>Pull needs no <code>[ship]</code> section and no SSH configuration — you are already on the box.</p>","path":["Deploy Modes"],"tags":[]},{"location":"deploy-modes/#ship","level":2,"title":"Ship","text":"<p>Build locally or in CI, then hand the finished artifacts to a lean remote.</p> <pre><code>fmd deploy ship --config site.toml\n</code></pre> <p><code>deploy ship</code> requires a <code>[ship]</code> section and proceeds in four phases:</p> <ol> <li>Build — <code>release create</code> runs in Docker (the fmd runner image) on your local    machine or CI runner, producing an immutable <code>release_YYYYMMDD_HHMMSS/</code> directory.</li> <li>Transfer — the release and its config snapshot are <code>rsync</code>-ed to the remote.</li> <li>Configure — <code>release configure</code> runs on the remote via <code>uvx</code>, wiring the bench    layout around the pre-built artifacts (no git access needed).</li> <li>Switch — <code>release switch</code> runs on the remote via <code>uvx</code> to activate the release    atomically (backup / migrate / maintenance behavior per <code>[switch]</code>).</li> </ol> <p>The remote only needs SSH, rsync, and uv — fmd itself is fetched and run there via <code>uvx</code>, so no build toolchain or git access is required on the target.</p>","path":["Deploy Modes"],"tags":[]},{"location":"deploy-modes/#ship-configuration","level":3,"title":"<code>[ship]</code> configuration","text":"<pre><code>[ship]\nhost = \"203.0.113.10\"          # remote SSH host\nssh_user = \"frappe\"            # default: frappe\nssh_port = 22                  # default: 22\nremote_path = \"\"               # empty = auto-detect $HOME/frappe/sites/&lt;site&gt;\nfmd_source = \"\"                # git URL/local path to run fmd via uvx; empty = auto\nrsync_options = []             # extra rsync flags\n</code></pre> <p>See <code>example-config.toml</code> at the repo root for the fully annotated schema, and Configuration for how <code>[ship]</code> fits with the rest of the config.</p>","path":["Deploy Modes"],"tags":[]},{"location":"deploy-modes/#choosing-a-mode","level":2,"title":"Choosing a mode","text":"<ul> <li>Prefer pull when the target server can build (adequate CPU/RAM, git/registry access)   and you want the simplest path — one command, no SSH plumbing.</li> <li>Prefer ship when you want to keep the target lean, build on a beefier CI runner or   local machine, or deploy the same pre-built release to more than one server.</li> </ul> <p>Ship builds once and can reuse the same release across multiple targets by pointing different <code>[ship]</code> configs at it.</p>","path":["Deploy Modes"],"tags":[]},{"location":"deploy-modes/#next-steps","level":2,"title":"Next steps","text":"<ul> <li>Automate either mode with GitHub Actions.</li> <li>Understand releases and the symlink switch in Concepts.</li> <li>Tune switch behavior and app builds in Configuration.</li> </ul>","path":["Deploy Modes"],"tags":[]},{"location":"faq/","level":1,"title":"FAQ","text":"","path":["FAQ"],"tags":[]},{"location":"faq/#what-is-fmd","level":2,"title":"What is fmd?","text":"<p>fmd (Frappe Manager Deployer) layers zero-downtime deployments on top of an existing Frappe Manager (FM) bench: atomic timestamped releases, instant rollback, and automated migrations.</p>","path":["FAQ"],"tags":[]},{"location":"faq/#how-does-fmd-relate-to-frappe-manager","level":2,"title":"How does fmd relate to Frappe Manager?","text":"<p>You need both. FM creates and manages the Frappe bench in Docker; fmd deploys versioned releases on top of that bench. fmd requires an existing FM bench — it does not replace FM.</p>","path":["FAQ"],"tags":[]},{"location":"faq/#which-python-version-do-i-need","level":2,"title":"Which Python version do I need?","text":"<p>Python &gt;=3.13,&lt;3.14. You also need Docker + Docker Compose v2.</p>","path":["FAQ"],"tags":[]},{"location":"faq/#can-i-use-fmd-on-windows","level":2,"title":"Can I use fmd on Windows?","text":"<p>Use WSL 2. fmd targets Linux and macOS.</p>","path":["FAQ"],"tags":[]},{"location":"faq/#how-do-i-access-private-github-repositories","level":2,"title":"How do I access private GitHub repositories?","text":"<p>Set a GitHub token with <code>repo</code> scope, via environment or config:</p> <pre><code>export GITHUB_TOKEN=ghp_your_token_here\n</code></pre> <pre><code>github_token = \"ghp_your_token_here\"\n</code></pre>","path":["FAQ"],"tags":[]},{"location":"faq/#whats-the-difference-between-pull-and-ship-mode","level":2,"title":"What's the difference between pull and ship mode?","text":"<ul> <li>pull: build on the target server and switch there (simplest; server needs build capacity).</li> <li>ship: build locally or in CI, rsync the release to the remote, then configure and switch there (offloads the build; remote needs SSH + rsync + uv).</li> </ul> <p>See Deploy modes.</p>","path":["FAQ"],"tags":[]},{"location":"faq/#what-happens-if-a-deployment-fails","level":2,"title":"What happens if a deployment fails?","text":"<p>The switch is atomic, so the site stays on the last working release. With <code>rollback = true</code> in <code>[switch]</code>, fmd auto-reverts on switch failure; otherwise the failed release is kept for debugging while the old one stays live.</p>","path":["FAQ"],"tags":[]},{"location":"faq/#how-many-releases-are-kept","level":2,"title":"How many releases are kept?","text":"<p><code>[release] releases_retain_limit</code> defaults to 7. Clean up manually with:</p> <pre><code>fmd cleanup mysite.localhost -r 3 -y\n</code></pre>","path":["FAQ"],"tags":[]},{"location":"faq/#can-i-roll-back-to-a-previous-release","level":2,"title":"Can I roll back to a previous release?","text":"<p>Yes, as long as it still exists:</p> <pre><code>fmd release list mysite.localhost\nfmd release switch mysite.localhost release_YYYYMMDD_HHMMSS\n</code></pre>","path":["FAQ"],"tags":[]},{"location":"faq/#can-i-sync-from-frappe-cloud","level":2,"title":"Can I sync from Frappe Cloud?","text":"<p>Yes. fmd can restore the latest FC database backup at switch time via <code>[switch] use_fc_db = true</code>, and import an app list. fmd deploys to self-hosted infrastructure; it syncs from FC, not to it. See Configuration.</p>","path":["FAQ"],"tags":[]},{"location":"faq/#how-do-i-change-a-sites-domain","level":2,"title":"How do I change a site's domain?","text":"<p>Use search-replace across the DB:</p> <pre><code>fmd search-replace mysite.localhost old.com new.com --dry-run\nfmd search-replace mysite.localhost old.com new.com\n</code></pre>","path":["FAQ"],"tags":[]},{"location":"faq/#how-do-i-enable-debug-logging","level":2,"title":"How do I enable debug logging?","text":"<p>Put <code>-v</code> before the subcommand:</p> <pre><code>fmd -v deploy pull --config site.toml\n</code></pre>","path":["FAQ"],"tags":[]},{"location":"faq/#where-can-i-get-help","level":2,"title":"Where can I get help?","text":"<ul> <li>Docs: https://rtcamp.github.io/fmd/</li> <li>Issues: https://github.com/rtcamp/fmd/issues</li> <li>Discussions: https://github.com/rtcamp/fmd/discussions</li> </ul>","path":["FAQ"],"tags":[]},{"location":"getting-started/","level":1,"title":"Getting started","text":"<p>fmd (Frappe Manager Deployer) layers zero-downtime, release-based deploys on top of an existing Frappe Manager (FM) bench. This page covers requirements, installation, and your first deployment.</p>","path":["Getting started"],"tags":[]},{"location":"getting-started/#requirements","level":2,"title":"Requirements","text":"<ul> <li>Python &gt;=3.13,&lt;3.14</li> <li>Docker with Compose v2 (the <code>docker compose</code> command, not the legacy <code>docker-compose</code>)</li> <li>Frappe Manager, with an existing FM bench for the site you will deploy</li> </ul> <p>Verify:</p> <pre><code>python3 --version\ndocker compose version\nfm --version\n</code></pre> <p>If you don't have an FM bench yet, create one first:</p> <pre><code>fm create mysite --environment prod   # bench at ~/frappe/sites/mysite/\n</code></pre>","path":["Getting started"],"tags":[]},{"location":"getting-started/#install","level":2,"title":"Install","text":"uv (recommended)pipxFrom source <pre><code>uv tool install frappe-deployer\n</code></pre> <pre><code>pipx install frappe-deployer\n</code></pre> <pre><code>git clone https://github.com/rtcamp/fmd.git\ncd fmd\npip install -e .\n</code></pre> <p>Verify:</p> <pre><code>fmd --version\n</code></pre> <p>GitHub token for private repos</p> <p>To clone private app repositories (and avoid API rate limits), provide a GitHub token via the <code>GITHUB_TOKEN</code> environment variable or the <code>github_token</code> key in your config file:</p> <pre><code>export GITHUB_TOKEN=ghp_your_token_here\n</code></pre>","path":["Getting started"],"tags":[]},{"location":"getting-started/#quick-start","level":2,"title":"Quick start","text":"","path":["Getting started"],"tags":[]},{"location":"getting-started/#1-configure-the-release-layout-one-time","level":3,"title":"1. Configure the release layout (one-time)","text":"<p>Convert your FM bench to the fmd release layout. Run this once per site:</p> <pre><code>fmd release configure mysite.localhost\n</code></pre>","path":["Getting started"],"tags":[]},{"location":"getting-started/#2-deploy-frappe-erpnext","level":3,"title":"2. Deploy Frappe + ERPNext","text":"<pre><code>fmd deploy pull mysite.localhost \\\n  --app frappe/frappe:version-15 \\\n  --app frappe/erpnext:version-15 \\\n  --maintenance-mode \\\n  --backups\n</code></pre> <p>This builds a new timestamped release, backs up the DB, runs migrations under maintenance mode, and switches the live symlink to the new release atomically.</p>","path":["Getting started"],"tags":[]},{"location":"getting-started/#3-verify","level":3,"title":"3. Verify","text":"<pre><code>fmd release list mysite.localhost   # list all releases\nfmd release info mysite.localhost   # inspect each app's repo/branch/commit/tag\n</code></pre>","path":["Getting started"],"tags":[]},{"location":"getting-started/#config-file-alternative","level":3,"title":"Config-file alternative","text":"<p>For repeatable, version-controlled deploys, put the flags in a <code>site.toml</code>:</p> <pre><code>site_name = \"mysite.localhost\"\nbench_name = \"mysite\"\ngithub_token = \"ghp_your_token\"  # optional, for private repos\n\n[[apps]]\nrepo = \"frappe/frappe\"\nref = \"version-15\"\n\n[[apps]]\nrepo = \"frappe/erpnext\"\nref = \"version-15\"\n\n[switch]\nmaintenance_mode = true\nbackups = true\n</code></pre> <p>Then deploy with:</p> <pre><code>fmd deploy pull --config site.toml\n</code></pre>","path":["Getting started"],"tags":[]},{"location":"getting-started/#next-steps","level":2,"title":"Next steps","text":"<ul> <li>Concepts — releases, atomic symlink switch, rollback, directory layout</li> <li>Configuration — all config sections, apps, hooks, and precedence</li> <li>Deploy modes — pull vs ship</li> </ul>","path":["Getting started"],"tags":[]},{"location":"github-actions/","level":1,"title":"GitHub Actions","text":"<p>Deploy from CI with the composite action <code>rtcamp/frappe-deployer@main</code>. Pick a strategy via <code>command</code>:</p> <ul> <li><code>command: pull</code> — SSH into the remote server and run <code>fmd deploy pull</code> there (server   builds and switches).</li> <li><code>command: ship</code> — build the release in the runner (Docker), rsync it to the remote,   then configure + switch remotely. Needs Docker Buildx and a <code>[ship]</code> config section.</li> </ul> <p>See Deploy modes for the trade-offs and Configuration for the TOML.</p>","path":["GitHub Actions"],"tags":[]},{"location":"github-actions/#secrets-and-variables","level":2,"title":"Secrets and variables","text":"Name Kind Description <code>GH_TOKEN</code> secret GitHub token for private app repos <code>SSH_PRIVATE_KEY</code> secret Private deploy key for the remote server <code>SSH_SERVER</code> secret Remote hostname or IP (or use <code>[ship].host</code>) <code>SSH_USER</code> secret SSH user, e.g. <code>frappe</code> (or use <code>[ship].ssh_user</code>) <code>SITE_NAME</code> variable Frappe site name (pull; ship reads <code>site_name</code> from TOML)","path":["GitHub Actions"],"tags":[]},{"location":"github-actions/#generate-a-deploy-key","level":3,"title":"Generate a deploy key","text":"<pre><code>ssh-keygen -t ed25519 -C \"fmd-deploy\" -f fmd_deploy -N \"\"\n</code></pre> <p>Add the public key (<code>fmd_deploy.pub</code>) to the server's <code>~/.ssh/authorized_keys</code> for <code>SSH_USER</code>, and store the private key (<code>fmd_deploy</code>) as the <code>SSH_PRIVATE_KEY</code> secret.</p>","path":["GitHub Actions"],"tags":[]},{"location":"github-actions/#pull-workflow","level":2,"title":"Pull workflow","text":"<p><code>.github/workflows/deploy-pull.yml</code>:</p> <pre><code>name: Deploy (pull)\n\non:\n  push:\n    branches: [main]\n  workflow_dispatch:\n\njobs:\n  deploy:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: rtcamp/frappe-deployer@main\n        with:\n          command: pull\n          sitename: ${{ vars.SITE_NAME }}\n          config_path: .github/configs/site.toml\n          gh_token: ${{ secrets.GH_TOKEN }}\n          ssh_private_key: ${{ secrets.SSH_PRIVATE_KEY }}\n          ssh_server: ${{ secrets.SSH_SERVER }}\n          ssh_user: ${{ secrets.SSH_USER }}\n</code></pre>","path":["GitHub Actions"],"tags":[]},{"location":"github-actions/#ship-workflow","level":2,"title":"Ship workflow","text":"<p>Ship builds in the runner, so add Docker Buildx. The site name comes from the TOML, which must include a <code>[ship]</code> section pointing at the remote:</p> <pre><code>site_name = \"mysite.example.com\"\n\n[ship]\nhost = \"192.168.1.100\"\nssh_user = \"frappe\"\nssh_port = 22\n</code></pre> <p><code>.github/workflows/deploy-ship.yml</code>:</p> <pre><code>name: Deploy (ship)\n\non:\n  push:\n    branches: [main]\n  workflow_dispatch:\n\njobs:\n  deploy:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: docker/setup-buildx-action@v3\n\n      - uses: rtcamp/frappe-deployer@main\n        with:\n          command: ship\n          config_path: .github/configs/site.toml\n          gh_token: ${{ secrets.GH_TOKEN }}\n          ssh_private_key: ${{ secrets.SSH_PRIVATE_KEY }}\n          ssh_server: ${{ secrets.SSH_SERVER }}\n          ssh_user: ${{ secrets.SSH_USER }}\n</code></pre>","path":["GitHub Actions"],"tags":[]},{"location":"github-actions/#inputs","level":2,"title":"Inputs","text":"<p>Common inputs: <code>command</code>, <code>config_path</code> (or inline <code>config_content</code>, plus <code>config_overrides</code> for per-environment merges), <code>sitename</code> (pull), <code>gh_token</code>, <code>ssh_private_key</code>, <code>ssh_server</code>, <code>ssh_user</code>, <code>ssh_port</code>. Switch overrides: <code>migrate</code>, <code>migrate_timeout</code>, <code>maintenance_mode</code>, <code>maintenance_mode_phases</code>, <code>backups</code>, <code>rollback</code>. Ship-only: <code>existing_release</code>, <code>skip_rsync</code>.</p> <p>For the full, authoritative input reference and defaults, see <code>action.yml</code> at the repo root.</p>","path":["GitHub Actions"],"tags":[]},{"location":"troubleshooting/","level":1,"title":"Troubleshooting","text":"<p>Common fmd-specific issues and fixes. For more detail on any command, run <code>fmd &lt;command&gt; --help</code>, and re-run with verbose logging (see below) to see what fmd is doing.</p>","path":["Troubleshooting"],"tags":[]},{"location":"troubleshooting/#verbose-logs","level":2,"title":"Verbose logs","text":"<p>Add <code>-v</code> before the subcommand to get detailed logs:</p> <pre><code>fmd -v deploy pull --config site.toml\n</code></pre> <p>This is the first thing to try for almost any failure.</p>","path":["Troubleshooting"],"tags":[]},{"location":"troubleshooting/#private-repository-access","level":2,"title":"Private repository access","text":"<p>Cloning private app repos during the build fails without a token:</p> <pre><code>ERROR: Repository access denied\n</code></pre> <p>Set a GitHub token with <code>repo</code> scope, via config or environment:</p> <pre><code>github_token = \"ghp_xxx\"\n</code></pre> <pre><code>export GITHUB_TOKEN=ghp_xxx\nfmd deploy pull --config site.toml\n\n# Test the token\ncurl -H \"Authorization: token $GITHUB_TOKEN\" https://api.github.com/user\n</code></pre>","path":["Troubleshooting"],"tags":[]},{"location":"troubleshooting/#config-validation-invalid-toml","level":2,"title":"Config validation / invalid TOML","text":"<p>Config errors are usually a TOML syntax problem. Validate the file directly:</p> <pre><code>python3 -c \"import tomllib; tomllib.load(open('site.toml', 'rb'))\"\n</code></pre> <p>Common mistakes:</p> <ul> <li>Missing quotes: <code>repo = frappe/frappe</code> (wrong) vs <code>repo = \"frappe/frappe\"</code> (correct)</li> <li><code>[[apps]</code> instead of <code>[[apps]]</code></li> <li>Unescaped backslashes in Windows paths</li> </ul>","path":["Troubleshooting"],"tags":[]},{"location":"troubleshooting/#frappe-cloud-credentials","level":2,"title":"Frappe Cloud credentials","text":"<p>FC sync fails with auth errors when credentials or names are wrong. Verify API access and that the names match exactly:</p> <pre><code>curl -u fc_key:fc_secret \\\n  https://frappecloud.com/api/method/press.api.bench.apps\n</code></pre> <pre><code>[fc]\napi_key = \"...\"\napi_secret = \"...\"\nsite_name = \"mysite.frappe.cloud\"   # must match the FC site name\nteam_name = \"my-team\"               # optional; must match the FC team slug\n</code></pre>","path":["Troubleshooting"],"tags":[]},{"location":"troubleshooting/#symlink-monorepo-app-not-found","level":2,"title":"Symlink / monorepo app not found","text":"<p>A monorepo app fails to build:</p> <pre><code>ERROR: Subdir path 'apps/my-app' not found in repo\n</code></pre> <p>The <code>subdir_path</code> must exist in the repo at the given <code>ref</code>, and <code>symlink = true</code> is required for subdir apps:</p> <pre><code>[[apps]]\nrepo = \"my-org/monorepo\"\nref = \"main\"\nsubdir_path = \"apps/my-app\"   # must exist at this ref\nsymlink = true\n</code></pre> <p>Verify the path exists in the repo before deploying.</p>","path":["Troubleshooting"],"tags":[]},{"location":"troubleshooting/#worker-drain-timeouts","level":2,"title":"Worker drain timeouts","text":"<p>If workers don't finish their jobs within the drain window during a switch, raise the timeouts in <code>[switch]</code>:</p> <pre><code>[switch]\ndrain_workers = true\ndrain_workers_timeout = 900   # default 300\nworker_kill_timeout = 30      # force-kill after this\n</code></pre> <p>For critical background jobs, confirm workers are idle before deploying:</p> <pre><code># FM mode\ndocker exec -it &lt;container&gt; supervisorctl status\n\n# Host mode\nsupervisorctl status | grep rq\n</code></pre>","path":["Troubleshooting"],"tags":[]},{"location":"troubleshooting/#inspecting-the-current-release","level":2,"title":"Inspecting the current release","text":"<p>To see which release is live and what each app was built from:</p> <pre><code>fmd release list mysite.localhost\nfmd release info mysite.localhost\nreadlink ~/frappe/sites/mysite.localhost/workspace/frappe-bench\n</code></pre> <p>The switch is atomic: the symlink only moves on success, so a failed switch leaves the old release live. Set <code>rollback = true</code> in <code>[switch]</code> to auto-revert on switch failure.</p>","path":["Troubleshooting"],"tags":[]},{"location":"commands/","level":1,"title":"Commands","text":"<p>Overview of the <code>fmd</code> CLI. Per-command detail pages are auto-generated from the CLI by <code>scripts/update_cli_docs.py</code>; for authoritative, up-to-date options run <code>fmd &lt;command&gt; --help</code>.</p> <p>Global options</p> <ul> <li><code>fmd -v &lt;command&gt;</code> — verbose logging (the <code>-v/--verbose</code> flag must precede the subcommand)</li> <li><code>-V, --version</code> — print the version</li> <li><code>-c, --config &lt;path&gt;</code> — load a TOML config file (accepted by every command)</li> </ul> <p>Commands take either a positional <code>&lt;site&gt;</code>/bench name or <code>--config site.toml</code>.</p>","path":["Commands"],"tags":[]},{"location":"commands/#command-tree","level":2,"title":"Command tree","text":"<ul> <li> <p> Deploy</p> <p>End-to-end deployment workflows.</p> <ul> <li><code>deploy pull</code> — configure (if needed) → create → switch on the target server</li> <li><code>deploy ship</code> — build locally/CI → rsync → configure + switch on the remote</li> </ul> <pre><code>fmd deploy pull --config site.toml\nfmd deploy ship --config site.toml\n</code></pre> </li> <li> <p> Release</p> <p>Manual control over the release lifecycle.</p> <ul> <li><code>release configure</code> — convert an FM bench to the fmd release layout (one-time)</li> <li><code>release create</code> — build a release without changing the live site</li> <li><code>release switch</code> — atomically activate a release</li> <li><code>release list</code> — list releases</li> <li><code>release shell</code> — interactive shell inside a release's build container</li> <li><code>release info</code> — inspect each app's git repo/branch/commit/tag</li> </ul> <pre><code>fmd release create mysite.localhost\nfmd release switch mysite.localhost release_YYYYMMDD_HHMMSS\nfmd release info mysite.localhost\n</code></pre> </li> <li> <p> Remote worker</p> <p>Run workers on separate servers.</p> <ul> <li><code>remote-worker enable</code> — open Redis/MariaDB ports for remote workers</li> <li><code>remote-worker sync</code> — sync the active release to a remote worker</li> </ul> <pre><code>fmd remote-worker enable mysite.localhost --rw-server 192.168.1.100\nfmd remote-worker sync mysite.localhost --rw-server 192.168.1.100\n</code></pre> </li> <li> <p> Search / replace</p> <p>DB-wide string replace, e.g. rewrite a site URL.</p> <pre><code>fmd search-replace mysite.localhost old.com new.com --dry-run\nfmd search-replace mysite.localhost old.com new.com\n</code></pre> </li> <li> <p> Cleanup</p> <p>Remove old releases and backups to free disk space.</p> <pre><code>fmd cleanup mysite.localhost -r 3 -b 5 -y --show-sizes\n</code></pre> </li> </ul> <p>See Concepts for the release model and Configuration for the TOML options each command reads.</p>","path":["Commands"],"tags":[]},{"location":"commands/cleanup/","level":1,"title":"Cleanup","text":"","path":["Commands","Cleanup"],"tags":[]},{"location":"commands/cleanup/#fmd-cleanup","level":2,"title":"<code>fmd cleanup</code>","text":"<p>Cleanup deployment backups and releases.</p> <p>Usage:</p> <pre><code>$ fmd cleanup\n</code></pre>","path":["Commands","Cleanup"],"tags":[]},{"location":"commands/cleanup/#examples","level":2,"title":"Examples","text":"","path":["Commands","Cleanup"],"tags":[]},{"location":"commands/cleanup/#cleanup-and-auto-approve","level":3,"title":"Cleanup and auto-approve","text":"<p>Runs cleanup without prompting for confirmation. Useful in CI or scripts.</p> <pre><code>fmd cleanup --config {config_path} --yes\n</code></pre>","path":["Commands","Cleanup"],"tags":[]},{"location":"commands/cleanup/#keep-last-3-releases","level":3,"title":"Keep last 3 releases","text":"<p>Deletes all but the 3 most recent releases in the bench workspace.</p> <pre><code>fmd cleanup mysite --release-retain-limit 3\n</code></pre>","path":["Commands","Cleanup"],"tags":[]},{"location":"commands/deploy/","level":1,"title":"Deploy","text":"","path":["Commands","Deploy"],"tags":[]},{"location":"commands/deploy/#fmd-deploy","level":2,"title":"<code>fmd deploy</code>","text":"<p>Deploy commands.</p> <p>Usage:</p> <pre><code>$ fmd deploy [OPTIONS] COMMAND [ARGS]...\n</code></pre> <p>Options:</p> <ul> <li><code>--help</code>: Show this message and exit.</li> </ul> <p>Commands:</p> <ul> <li><code>pull</code>: Full deploy: configure (if needed) → create release → switch.</li> <li><code>ship</code>: Ship deploy: create release locally → rsync to remote → switch on remote.</li> </ul>","path":["Commands","Deploy"],"tags":[]},{"location":"commands/deploy/#fmd-deploy-pull","level":3,"title":"<code>fmd deploy pull</code>","text":"<p>Full deploy: configure (if needed) → create release → switch.</p> <p>Usage:</p> <pre><code>$ fmd deploy pull\n</code></pre>","path":["Commands","Deploy"],"tags":[]},{"location":"commands/deploy/#examples","level":2,"title":"Examples","text":"","path":["Commands","Deploy"],"tags":[]},{"location":"commands/deploy/#deploy-with-frappe-cloud-integration","level":3,"title":"Deploy with Frappe Cloud integration","text":"<p>Pulls FC app list and DB backup, then deploys. Requires FC API credentials.</p> <pre><code>fmd deploy pull {bench_name} --fc-key {fc_key} --fc-secret {fc_secret} --fc-site {fc_site}\n</code></pre>","path":["Commands","Deploy"],"tags":[]},{"location":"commands/deploy/#deploy-with-explicit-apps","level":3,"title":"Deploy with explicit apps","text":"<p>Overrides config apps list with specified repos and refs.</p> <pre><code>fmd deploy pull mysite --app frappe/frappe:version-15 --app frappe/erpnext:version-15\n</code></pre>","path":["Commands","Deploy"],"tags":[]},{"location":"commands/deploy/#deploy-from-config-file","level":3,"title":"Deploy from config file","text":"<p>Reads bench name and app list from the TOML config file. Full deploy: configure → create → switch.</p> <pre><code>fmd deploy pull --config {config_path}\n</code></pre>","path":["Commands","Deploy"],"tags":[]},{"location":"commands/deploy/#fmd-deploy-ship","level":3,"title":"<code>fmd deploy ship</code>","text":"<p>Ship deploy: create release locally → rsync to remote → switch on remote.</p> <p>Usage:</p> <pre><code>$ fmd deploy ship\n</code></pre>","path":["Commands","Deploy"],"tags":[]},{"location":"commands/deploy/#examples_1","level":2,"title":"Examples","text":"","path":["Commands","Deploy"],"tags":[]},{"location":"commands/deploy/#skip-rsync-release-already-on-remote","level":3,"title":"Skip rsync (release already on remote)","text":"<p>Switches to an already-transferred release without rsyncing again. Useful for retrying a failed switch.</p> <pre><code>fmd deploy ship --config {config_path} --existing-release {release_name} --skip-rsync\n</code></pre>","path":["Commands","Deploy"],"tags":[]},{"location":"commands/deploy/#ship-using-an-existing-release","level":3,"title":"Ship using an existing release","text":"<p>Skips release creation and rsyncs an already-created local release to the remote, then switches.</p> <pre><code>fmd deploy ship --config {config_path} --existing-release {release_name}\n</code></pre>","path":["Commands","Deploy"],"tags":[]},{"location":"commands/deploy/#ship-from-config-file","level":3,"title":"Ship from config file","text":"<p>Creates a release locally, rsyncs it to the remote server, and switches the live bench. Requires a [ship] section in config.</p> <pre><code>fmd deploy ship --config {config_path}\n</code></pre>","path":["Commands","Deploy"],"tags":[]},{"location":"commands/release/","level":1,"title":"Release","text":"","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#fmd-release","level":2,"title":"<code>fmd release</code>","text":"<p>Release commands.</p> <p>Usage:</p> <pre><code>$ fmd release [OPTIONS] COMMAND [ARGS]...\n</code></pre> <p>Options:</p> <ul> <li><code>--help</code>: Show this message and exit.</li> </ul> <p>Commands:</p> <ul> <li><code>configure</code>: One-time setup: converts a plain bench into a versioned release structure.</li> <li><code>create</code>: Create a new release: clone apps, build assets, no live bench changes.</li> <li><code>switch</code>: Switch live bench symlink to a previously-created release.</li> <li><code>list</code>: List all releases, marking the currently active one.</li> <li><code>shell</code>: Open an interactive shell inside the build container for a release.</li> <li><code>info</code>: Show release info by inspecting each app's git repository.</li> </ul>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#fmd-release-configure","level":3,"title":"<code>fmd release configure</code>","text":"<p>One-time setup: converts a plain bench into a versioned release structure.</p> <p>Usage:</p> <pre><code>$ fmd release configure\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#examples","level":2,"title":"Examples","text":"","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#configure-from-config-file","level":3,"title":"Configure from config file","text":"<p>Reads bench name and apps from the TOML config and runs one-time release structure setup.</p> <pre><code>fmd release configure --config {config_path}\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#configure-from-bench-name","level":3,"title":"Configure from bench name","text":"<p>Converts a plain bench into a versioned release structure using bench name to locate it.</p> <pre><code>fmd release configure mysite\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#fmd-release-create","level":3,"title":"<code>fmd release create</code>","text":"<p>Create a new release: clone apps, build assets, no live bench changes.</p> <p>Usage:</p> <pre><code>$ fmd release create\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#examples_1","level":2,"title":"Examples","text":"","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#create-with-explicit-apps","level":3,"title":"Create with explicit apps","text":"<p>Overrides the apps list from config with the specified repos and refs.</p> <pre><code>fmd release create mysite --app frappe/frappe:version-15 --app frappe/erpnext:version-15\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#create-in-image-mode-with-build-dir","level":3,"title":"Create in image mode with build dir","text":"<p>Builds the release as a standalone directory outside the bench. Activates image mode automatically.</p> <pre><code>fmd release create {bench_name} --build-dir {build_dir}\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#create-from-config-file","level":3,"title":"Create from config file","text":"<p>Clones apps, builds assets, and writes a new release directory. No live bench changes.</p> <pre><code>fmd release create --config {config_path}\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#fmd-release-switch","level":3,"title":"<code>fmd release switch</code>","text":"<p>Switch live bench symlink to a previously-created release.</p> <p>Usage:</p> <pre><code>$ fmd release switch\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#examples_2","level":2,"title":"Examples","text":"","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#switch-with-maintenance-mode","level":3,"title":"Switch with maintenance mode","text":"<p>Enables maintenance mode during the switch to prevent user-facing errors while the symlink is updated.</p> <pre><code>fmd release switch {bench_name} {release_name} --maintenance-mode\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#switch-with-migrate","level":3,"title":"Switch with migrate","text":"<p>Runs bench migrate after switching the symlink to apply any pending schema changes.</p> <pre><code>fmd release switch {bench_name} {release_name} --migrate\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#switch-to-a-release","level":3,"title":"Switch to a release","text":"<p>Updates the live bench symlink to point at the specified release directory.</p> <pre><code>fmd release switch {bench_name} {release_name}\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#fmd-release-list","level":3,"title":"<code>fmd release list</code>","text":"<p>List all releases, marking the currently active one.</p> <p>Usage:</p> <pre><code>$ fmd release list\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#examples_3","level":2,"title":"Examples","text":"","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#list-from-config-file","level":3,"title":"List from config file","text":"<p>Reads bench name from config and lists all releases with metadata.</p> <pre><code>fmd release list --config {config_path}\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#list-releases-by-bench-name","level":3,"title":"List releases by bench name","text":"<p>Lists all releases in the bench workspace, marking the currently active one.</p> <pre><code>fmd release list mysite\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#fmd-release-shell","level":3,"title":"<code>fmd release shell</code>","text":"<p>Open an interactive shell inside the build container for a release.</p> <p>Usage:</p> <pre><code>$ fmd release shell\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#examples_4","level":2,"title":"Examples","text":"","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#shell-into-latest-release-from-config","level":3,"title":"Shell into latest release from config","text":"<p>Finds the latest release and opens an interactive shell in the build container.</p> <pre><code>fmd release shell --config {config_path}\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#shell-into-a-specific-release","level":3,"title":"Shell into a specific release","text":"<p>Opens an interactive shell for a specific release directory.</p> <pre><code>fmd release shell --config {config_path} {release_name}\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#fmd-release-info","level":3,"title":"<code>fmd release info</code>","text":"<p>Show release info by inspecting each app's git repository.</p> <p>Usage:</p> <pre><code>$ fmd release info\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#examples_5","level":2,"title":"Examples","text":"","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#info-from-config-file","level":3,"title":"Info from config file","text":"<p>Reads bench path from config and shows git info for all apps.</p> <pre><code>fmd release info --config {config_path}\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/release/#info-by-bench-name","level":3,"title":"Info by bench name","text":"<p>Inspects each app's git repository in the bench and prints commit, branch, and tag info.</p> <pre><code>fmd release info mysite\n</code></pre>","path":["Commands","Release"],"tags":[]},{"location":"commands/remote-worker/","level":1,"title":"Remote worker","text":"","path":["Commands","Remote worker"],"tags":[]},{"location":"commands/remote-worker/#fmd-remote-worker","level":2,"title":"<code>fmd remote-worker</code>","text":"<p>Remote-Worker commands.</p> <p>Usage:</p> <pre><code>$ fmd remote-worker [OPTIONS] COMMAND [ARGS]...\n</code></pre> <p>Options:</p> <ul> <li><code>--help</code>: Show this message and exit.</li> </ul> <p>Commands:</p> <ul> <li><code>enable</code>: Enable remote worker: expose DB + Redis ports, create worker site configs.</li> <li><code>sync</code>: Sync workspace to remote worker server.</li> </ul>","path":["Commands","Remote worker"],"tags":[]},{"location":"commands/remote-worker/#fmd-remote-worker-enable","level":3,"title":"<code>fmd remote-worker enable</code>","text":"<p>Enable remote worker: expose DB + Redis ports, create worker site configs.</p> <p>Usage:</p> <pre><code>$ fmd remote-worker enable\n</code></pre>","path":["Commands","Remote worker"],"tags":[]},{"location":"commands/remote-worker/#examples","level":2,"title":"Examples","text":"","path":["Commands","Remote worker"],"tags":[]},{"location":"commands/remote-worker/#enable-from-config-file","level":3,"title":"Enable from config file","text":"<p>Reads bench name and remote worker settings from config, exposes DB/Redis ports and writes worker site configs.</p> <pre><code>fmd remote-worker enable --config {config_path}\n</code></pre>","path":["Commands","Remote worker"],"tags":[]},{"location":"commands/remote-worker/#enable-with-bench-name","level":3,"title":"Enable with bench name","text":"<p>Enables remote worker for the specified bench, pointing at the given remote server IP.</p> <pre><code>fmd remote-worker enable {bench_name} --rw-server {rw_server}\n</code></pre>","path":["Commands","Remote worker"],"tags":[]},{"location":"commands/remote-worker/#fmd-remote-worker-sync","level":3,"title":"<code>fmd remote-worker sync</code>","text":"<p>Sync workspace to remote worker server.</p> <p>Usage:</p> <pre><code>$ fmd remote-worker sync\n</code></pre>","path":["Commands","Remote worker"],"tags":[]},{"location":"commands/remote-worker/#examples_1","level":2,"title":"Examples","text":"","path":["Commands","Remote worker"],"tags":[]},{"location":"commands/remote-worker/#sync-from-config-file","level":3,"title":"Sync from config file","text":"<p>Reads bench name and remote worker settings from config and syncs the workspace to the remote server.</p> <pre><code>fmd remote-worker sync --config {config_path}\n</code></pre>","path":["Commands","Remote worker"],"tags":[]},{"location":"commands/remote-worker/#sync-with-bench-name","level":3,"title":"Sync with bench name","text":"<p>Syncs the workspace for the specified bench to the remote worker server.</p> <pre><code>fmd remote-worker sync {bench_name} --rw-server {rw_server}\n</code></pre>","path":["Commands","Remote worker"],"tags":[]},{"location":"commands/search-replace/","level":1,"title":"Search replace","text":"","path":["Commands","Search replace"],"tags":[]},{"location":"commands/search-replace/#fmd-search-replace","level":2,"title":"<code>fmd search-replace</code>","text":"<p>Search and replace text across all text fields in the Frappe database.</p> <p>Usage:</p> <pre><code>$ fmd search-replace\n</code></pre>","path":["Commands","Search replace"],"tags":[]},{"location":"commands/search-replace/#examples","level":2,"title":"Examples","text":"","path":["Commands","Search replace"],"tags":[]},{"location":"commands/search-replace/#dry-run-search-replace","level":3,"title":"Dry-run search-replace","text":"<p>Shows what would be changed without modifying the database.</p> <pre><code>fmd search-replace {bench_name} {search} {replace} --dry-run\n</code></pre>","path":["Commands","Search replace"],"tags":[]},{"location":"commands/search-replace/#search-and-replace-in-db","level":3,"title":"Search and replace in DB","text":"<p>Replaces all occurrences of the search text across all text fields in the Frappe database.</p> <pre><code>fmd search-replace {bench_name} {search} {replace}\n</code></pre>","path":["Commands","Search replace"],"tags":[]}]}